"""
app.py – Entry point for the Research Agent backend.

TODO: Wire up FastAPI server here with endpoints like:
  POST /chat/start   – Start a new conversation (sends initial_state to graph)
  POST /chat/resume  – Resume a paused graph (sends Command(resume=...) )
  GET  /health       – Health check

The LangGraph is already built and compiled in:
  src.Research_Agent.graph.graph_builder.compiled_graph
"""